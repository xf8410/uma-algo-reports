# 算法体系梳理与结合提升点（2026-09-17）

来源：你发的《赛马娘算法讨论记录 20260803-20260809》全文 + 记忆仓里的算法共识。这篇先把你的算法全部点一遍名，再回答那个核心问题：**这些算法里，哪些能提升我们现在的"结合算法"（GA 冠军 + 四优化器赛马 + 减噪链路）？**

---

## 一、你发过的算法清单（全量点名）

### A. 神经网络拓扑类（模型怎么搭）

| 算法 | 大白话 | 讨论记录里的评分 |
|------|--------|----------------|
| 多头编码器 + 共享躯干（拓扑A） | 不同输入各走各的门，最后汇合进一个主干 | 推荐方向之一 |
| GNN 图神经网络（拓扑B） | 把支援卡/属性当成"关系网"来学 | 推荐方向之一 |
| AlphaZero 残差塔（拓扑C） | 深层堆叠的经典棋类架构 | 推荐方向之一 |
| **分段线性编码（PLE）** | 把"1200属性分大涨"这种拐点直接告诉模型，不让它自己猜 | **9/10 必加** |
| Spline 激活函数 | 用可弯的曲线替代 ReLU 折线 | 4/10 不推荐（训练不稳） |
| C51 分布价值头 | 模型不只输出一个平均分，输出整条分数分布 | 7/10 二阶段 |
| MC Dropout / Deep Evidential | 用"模型的犹豫程度"来衡量不确定性 | Evidential 有两个坑，结论后来改了 |

### B. 搜索与决策类

| 算法 | 大白话 | 评分/状态 |
|------|--------|----------|
| MCTS 确定性展开 | 知道种子就不用抽签，直接把结果算出来往下搜 | 核心洞察，待 RNG 可观测性 |
| 价值头截断 | NN 先给个预判，MCTS 只搜有戏的分支 | 7/10，等 NN 够准 |
| UCB 变体（分支选择） | 好奇心公式：常选的分高的，也照顾没试过的 | 8/10 |
| Thompson Sampling | 按胜率抽签做决定，拦截包预知成败后变无敌 | 待拦截管道 |
| POMCP / 粒子滤波→MCTS 条件化 | 用一堆"猜的状态"代替一个确定状态去搜 | 你的主路线 |

### C. 优化器类（搜参数）

| 算法 | 大白话 | 评分/状态 |
|------|--------|----------|
| **CMA-ES** | 不断调整"往哪试、步子多大"的自适应进化 | **9/10 必加 → 已在跑** |
| 贝叶斯优化 | 用"高斯过程代理模型"聪明猜下一个试验点 | ⭐⭐⭐⭐⭐，你用过它调噪声 |
| 符号回归 | 让公式自己长出来 | 6/10 辅助 |
| PBT 种群训练 | 边训练边淘汰换血 | 3/10 复杂度高 |
| 多臂老虎机（UCB） | 决定"每个选项花多少预算" | 8/10，直接对口减噪 |
| 模拟退火 | 高温乱跳低温收敛 | 已在赛马 |
| 禁忌搜索 | 走过的路记小本本不再走 | 3/10 |

### D. RNG 信念推断（你的特色方向）

- **贝叶斯网络对 RNG 做信念推断**：种子空间以亿计，但伪随机是确定的——从观测状态反推种子候选集，随对局推进用后验更新收窄。
- **训练成败是客户端本地 RNG 现场算的**（记录里概率最高的判断）：拦截包只能看到事件结果（确定性、可预下发），看不到训练成败（roll 之前不存在）。
- **逆向工具链**：内存找答案 → 掩码减法（已知字段抠掉，剩嫌疑区）→ 纯黑盒差分（SL 两次对比）。

### E. 噪声与评分曲线

- **两种方差**：固有方差（游戏本身的随机，换什么算法都在）vs 认知方差（评估不够造成的）。
- **评分拐点编码**：1200 属性的分段大涨直接编码进输入层/门控，不让模型"发现"。

---

## 二、对当前"结合算法"的提升点（按优先级）

当前体系：手写策略 70 字段参数空间 + GA 冠军 warm-start + 四优化器赛马（CMA-ES/TPE/SA/CEM）+ CRN/100局/换种子验收减噪链路 + bench_base 黑盒模拟器。

### ① 多臂老虎机 / Racing 自动加注 —— 最直接对口，立即可做（推荐第一）

讨论记录 8/10 的 UCB，映射到我们体系就是**减噪四层的第三层**：
- 现状：所有候选用同一种局数评估，纯拼运气。
- 提升点：海选 20 局，只有"临界候选"（和当前最好差距在波动带内的）才加注到 100 局。数学依据正是 UCB1 / Successive Halving——**"数学上保证不差于纯均值"**，这句记录里的判断可以直接落地成 compare.py 的选优逻辑。
- 成本：几十行代码，不改模拟器，不碰评估口径。

### ② 贝叶斯优化进赛马 —— 第五家优化器（推荐第二）

- 你有真实使用经验（"我用贝叶斯优化了噪声"），TPE 本质就是贝叶斯优化的轻量替身，但真 BO（高斯过程代理）在 70 维上每轮 O(n³) 会贵。
- 提升点：用**高维 BO 变体**（HEBO / SaasBO 这类，专门治维度高的 BO）当第五家进赛马，和 CMA-ES 直接同规则对跑。它和 TPE 的差别=用真正的代理模型而不是排序，理论上样本效率更高。
- 前置：等 center 基准口径问题（8k 差异）排查完、赛马口径统一后再加，避免在坏地基上比。

### ③ CRN + 换种子验收 ↔ 记录里"两种方差"的对账（已落地，补理论对齐）

- 我们已落地的：CRN 同种子同考卷、100 局/卡、换种子验收（VAL_SEEDS 9001-9003）。
- 用记录的语言翻译：**100 局压的是固有方差的估计误差（√100=10 倍收缩），换种子验收杀的是认知方差（种子过拟合）**。这套组合正好是记录里"固有方差 vs 认知方差"两分法的工程实现，理论是通的，可以写进算法清单归档。

### ④ 贝叶斯网络 RNG 信念推断 —— 最高价值，但先决条件是 RNG 可观测性实测（中期）

- 记录里记录的核心判断："训练成败是客户端本地 RNG 现场算的"→ 模拟器 umaai-rs 的 RNG 也是同一套确定性实现 → **这就是 CRN 有效的根本原因**（同种子整条育成轨迹完全确定）。
- 提升点分两段：
  - 对 **uma-algo-lab（当前）**：不直接受益。优化器拿到的分数已经是模拟器确定性输出，没什么可推断的。
  - 对 **URA 实时决策线（中期）**：一旦"从游戏观测状态反推种子候选集"的管道做成，MCTS 搜索层做确定性展开（记录里的核心洞察），预测从"概率"变"预知"。这是整个体系里上限最高的一步，但必须先实测 RNG 可观测性（记忆规则：不脑补）。
- 结论：不阻塞 lab，列入 URA 线里程碑；等逆向工具链（掩码减法/黑盒差分）出观测数据再设计贝叶斯网络结构。

### ⑤ 分段线性编码 / 阈值门控 —— 9/10，但属于 NN 线不是本周任务

- 评分函数（曲线拐点）已经写在模拟器里，优化器侧不用动。
- 落点在 **NN 权重迭代线**（754 维蒸馏训练）的输入层，属于下一个里程碑。记录里"只改输入层，立刻知道拐点"的判断依然成立，等蒸馏基线稳定后加。

### ⑥ 明确不做的（记录里已判死/低分，防止复杂度爆炸）

- PBT（3/10）、HRL（2/10）、Ensemble（2/10）、Spline（4/10）、禁忌搜索（3/10）。
- 记录里的警告原话值得贴在墙上："**真正的问题不是加进去变差，是加进去不知道哪个环节坏了**"。一次只加一个，验证通过再下一个。

---

## 三、行动清单

| 优先级 | 动作 | 状态 |
|--------|------|------|
| 1 | Racing/UCB 自动加注进 compare.py（减噪第三层） | 待 center 口径排查完一起上 |
| 2 | 高维 BO（HEBO 类）当第五家进赛马 | 赛马口径统一后 |
| 3 | RNG 可观测性实测（URA 线前置） | 中期，不阻塞 lab |
| 4 | 分段线性编码进 NN 蒸馏输入层 | NN 权重迭代线下一步 |
| 5 | PBT/HRL/Ensemble/Spline | 不做 |

**一句话总结**：你的算法清单里，能直接给"结合算法"提速的是 UCB 加注（省钱省时）和高维 BO（样本效率）；上限最高的是 RNG 信念推断（把概率变预知），但它要先过 RNG 可观测性实测这一关；NN 拓扑类（PLE/C51）是另一条线的弹药，不在这条线上乱堆。
